# Asset Batch 01 — Core cast (for fleet generation via firstmate on gpu)

Follow `style-guide.md` strictly. All original characters. PNG + alpha, 1024px, neutral background, readable at 64px.

1. `cursor-spirit-idle` — the player: luminous cyan block-of-light spirit, soft glow, faint afterimage trail, terminal-blink feel. 3/4 view.
2. `cursor-spirit-dash` — same character mid-dash, stretched with motion trail (for w/b/e jumps).
3. `vimkin-base-trapped` — round ink-and-paper creature, pen-stroke hair tuft, lantern-belly DIM, tangled in scribble-strands, hopeful eyes.
4. `vimkin-base-rescued` — same creature, lantern-belly GLOWING amber, jumping with joy.
5. `vimkin-navigator` — variant with tiny folded paper hat (tier-2 zones).
6. `vimkin-grammar` — variant holding a small quote-mark staff (tier-4 zones).
7. `entropy-worm` — mischievous scribble-worm of tangled strikethrough lines, leaving stray typo letters behind. Playful villain, not scary.
8. `app-icon-vimkin` — app icon: a Vimkin's amber lantern-belly glow inside a dark rounded-square terminal frame with a cyan block cursor. Must read at 16px.

Output to `assets/generated/batch-01/` with provenance sidecars.
